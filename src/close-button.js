import styled from 'styled-components'

const CloseButton = styled.span`
  cursor: pointer;
  font-size: 20px;
  padding: 10px;
  position: absolute;
  top: 5px;
  right: 5px;
  z-index: 2;
`

export default CloseButton
